# Copy to deploy/.env and adjust.
# Compose project name (container/ network prefix).
COMPOSE_PROJECT_NAME=okuru

# Override the production image tag if needed.
# IMAGE=registry.gitlab.com/kurob1993/okuru

# GitLab registry path (used by CI push).
CI_PROJECT_PATH=kurob1993/okuru

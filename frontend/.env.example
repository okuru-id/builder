# Backend dev server target for Vite proxy. Copy to .env and adjust if APP_PORT changes.
# ponytail: defaults baked into vite.config.ts; this file only overrides.
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=3000
